# workstation
Personal Workstation setup automation, for work and home

Intallation:
```bash
sudo apt update && sudo apt install -y ansible git curl
git clone "this repo" (with recursive submodules)
cd "this repo"
./apply
sudo tailscale login
```

After finishing, make sure to authenticate tailscale
